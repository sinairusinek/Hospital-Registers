.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
.env
.claude/

# Private dataset — never commit. Public, redacted artifacts live in data/public/.
data/private/*
!data/private/README.md

# Datasette build artifacts
*.db
site/build/

# Local Google AI Studio export cache
Hospital Admission Register Sp _ Google AI Studio_files/

# Article + bibliography working files — never commit (private drafts, citations, extracts)
paper/

# Derived artifact: rebuild with python3 pipeline/build.py
data/public/hospital-registers-normalized.tsv

# Evaluation runs — working output, kept out of the public repo
data/eval/

# Harvested newspaper page text (122MB, over GitHub's per-file limit).
# Regenerate: python3 pipeline/jrayed_text_harvest.py — resumable, reads the
# hit lists in data/newspapers/ and refetches only what is missing.
data/newspapers/page_texts.jsonl
data/newspapers/heb_page_texts.jsonl

# Large regenerable full-text harvest (see data/newspapers/README.md)
data/newspapers/govhosp_pls4148_texts.jsonl
data/newspapers/heb_sample_texts.jsonl
